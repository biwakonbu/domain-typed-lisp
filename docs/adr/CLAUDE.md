# CLAUDE.md (docs/adr)

この階層は Architecture Decision Record。

## 運用ルール
- 新規 ADR は連番 `NNNN-*.md`（4桁ゼロ埋め）で追加する。
- 既存 ADR の結論を書き換える場合は、原則「改訂」ではなく「後続 ADR」で上書きする。
- 参照先（README / docs-site）に影響する場合はリンク整合を確認する。
