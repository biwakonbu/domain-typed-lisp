# CLAUDE.md (.github)

この階層は CI / リリース運用の定義を扱う。

## スコープ
- 対象: `.github/workflows/*.yml`

## 変更ルール
- ワークフローのジョブ名と目的が一致するように維持する。
- コマンドを変更したら、`README.md` と整合させる。
- docs 関連変更時は `docs-site.yml` の `paths` フィルタ漏れを確認する。

## 最低確認
- YAML 構文が崩れていないこと。
- 変更したジョブに対応するローカルコマンドが通ること。
