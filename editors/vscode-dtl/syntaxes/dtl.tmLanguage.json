{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "DTL",
  "scopeName": "source.dtl",
  "patterns": [
    {
      "include": "#comment"
    },
    {
      "include": "#string"
    },
    {
      "include": "#number"
    },
    {
      "include": "#logicVar"
    },
    {
      "include": "#surfaceTag"
    },
    {
      "include": "#typeKeyword"
    },
    {
      "include": "#booleanLiteral"
    },
    {
      "include": "#operator"
    },
    {
      "include": "#wildcard"
    },
    {
      "include": "#keyword"
    }
  ],
  "repository": {
    "comment": {
      "patterns": [
        {
          "name": "comment.line.semicolon.dtl",
          "match": ";.*$"
        }
      ]
    },
    "string": {
      "patterns": [
        {
          "name": "string.quoted.double.dtl",
          "match": "\"[^\"\\n]*\""
        }
      ]
    },
    "number": {
      "patterns": [
        {
          "name": "constant.numeric.integer.dtl",
          "match": "(?<![^\\s()])[-+]?\\d+(?![^\\s()])"
        }
      ]
    },
    "logicVar": {
      "patterns": [
        {
          "name": "variable.other.logic.dtl",
          "match": "(?<![^\\s()])\\?[^\\s()\";]+(?![^\\s()])"
        }
      ]
    },
    "surfaceTag": {
      "patterns": [
        {
          "name": "entity.other.attribute-name.tag.dtl",
          "match": "(?<![^\\s()])(?::alias|:canonical|:constructors|:args|:terms|:head|:body|:params|:formula|:values|:ret|:name|:summary|:path|:category|:from|:to|:source|:command|:required)(?![^\\s()])"
        }
      ]
    },
    "typeKeyword": {
      "patterns": [
        {
          "name": "storage.type.dtl",
          "match": "(?<![^\\s()])(?:Bool|Int|Symbol|Refine|Adt)(?![^\\s()])"
        }
      ]
    },
    "booleanLiteral": {
      "patterns": [
        {
          "name": "constant.language.boolean.dtl",
          "match": "(?<![^\\s()])(?:true|false)(?![^\\s()])"
        }
      ]
    },
    "operator": {
      "patterns": [
        {
          "name": "keyword.operator.function-type.dtl",
          "match": "(?<![^\\s()])->(?![^\\s()])"
        }
      ]
    },
    "wildcard": {
      "patterns": [
        {
          "name": "constant.language.wildcard.dtl",
          "match": "(?<![^\\s()])_(?![^\\s()])"
        }
      ]
    },
    "keyword": {
      "patterns": [
        {
          "name": "keyword.control.dtl",
          "match": "(?<![^\\s()])(?:import|sort|data|relation|fact|rule|assert|universe|defn|project|module|reference|contract|quality-gate|and|not|let|if|match)(?![^\\s()])"
        }
      ]
    }
  }
}
