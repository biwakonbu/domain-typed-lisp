# TODO

## P0 (直近で実施)
- [ ] `main` ブランチの CI 成功を確認する（GitHub Actions `ci`）。
- [ ] `main` の Branch protection を設定する（必須ステータスチェック: `ci`）。
- [x] README の Coverage バッジを実測値連動に切り替える（CodeCov）。
- [ ] `v0.1.0` タグを作成して MVP 完了点を固定する。

## P1 (次スプリント)
- [x] `dtl check` に JSON 出力オプションを追加する（機械可読診断）。
- [x] 診断メッセージの改善（エラーコードごとの修正ヒントを表示）。
- [x] 複数ファイル入力の最小設計を追加する（分割構成案）。
- [x] `dtl check <FILE>...` の複数ファイル入力を実装する（import 解決含む）。
- [x] E2E テストを追加する（`examples/` を対象に CLI 実行結果を固定）。

## P2 (中期)
- [x] import/モジュール解決の MVP 実装。
- [x] 含意判定強化（Refinement の否定・連言の網羅ケース拡張）。
- [x] パフォーマンス計測ベンチを追加する（ルール数・事実数スケーリング）。
- [x] リリースワークフローを追加する（タグ作成でバイナリ配布）。
