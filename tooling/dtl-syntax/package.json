{
  "name": "@domain-typed-lisp/dtl-syntax",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json",
    "generate": "bun run src/emit.ts",
    "check-generated": "bun run src/emit.ts --check",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.2",
    "typescript": "^5.8.2"
  }
}
